# Email Worker URL (defaults to production if not set)
EMAIL_WORKER_URL=https://terminus-email.terminusemail.workers.dev

# Base URL for metadata (used for Open Graph images)
NEXT_PUBLIC_BASE_URL=https://terminusindustrials.com
